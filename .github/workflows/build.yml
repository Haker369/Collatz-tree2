name: Build Windows EXE

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install numpy==1.26.4 matplotlib==3.8.4 PyQt5==5.15.10 pyinstaller==6.6.0

    - name: Compile to Single EXE file
      run: |
        pyinstaller --noconfirm --onefile --windowed --hidden-import="matplotlib.backends.backend_qt5agg" --name="CollatzTree" app.py

    - name: Upload Finished EXE
      uses: actions/upload-artifact@v4
      with:
        name: CollatzTree-Single-Executable
        path: dist/CollatzTree.exe
